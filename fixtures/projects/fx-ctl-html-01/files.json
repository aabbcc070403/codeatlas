{
  "package.json": "{\"name\":\"fx-ctl-html-01\",\"private\":true,\"dependencies\":{}}",
  "README.md": "# fx-ctl-html-01\n\n评论渲染模块（安全对照实现）。\n",
  "src/render.ts": "export function renderComment(el: HTMLElement, text: string): void {\n  // 纯文本渲染：不解析 HTML\n  el.textContent = text\n}\n\nexport function renderBadge(el: HTMLElement, label: string): void {\n  el.setAttribute('data-label', label)\n}",
  "src/index.ts": "export { renderComment, renderBadge } from './render'\n"
}
